import 'package:flutter/material.dart';

class ArrayGame extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar:AppBar(
        title: Text("Array Game"),
        centerTitle: true,
        backgroundColor: Colors.blue,
      )
      ,

    );
  }
}
