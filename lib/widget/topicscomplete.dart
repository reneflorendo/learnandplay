
import 'package:flutter/material.dart';

class Topics extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar:AppBar(
          title: Text("Topics Complete"),
          centerTitle: true,
          backgroundColor: Colors.blue,
        )
    );
  }
}
